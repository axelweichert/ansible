---
- hosts: master
  become: true
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
- hosts: nodes
  become: true
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
